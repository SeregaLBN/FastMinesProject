<Page
    x:Class="fmg.MosaicPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:fmg"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="Coral">

    <canvas:CanvasVirtualControl x:Name="_canvasVirtualControl"
                                 Height="{x:Bind MosaicField.WindowSize, Mode=OneWay, Converter={StaticResource SizeToHeightConverter}}"
                                 Width="{x:Bind  MosaicField.WindowSize, Mode=OneWay, Converter={StaticResource SizeToWidthConverter}}"
                                 RegionsInvalidated="OnRegionsInvalidated"
                                 ClearColor="Bisque"
                                 Margin="5,5,5,5" />

</Page>
